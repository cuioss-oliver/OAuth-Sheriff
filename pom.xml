<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>de.cuioss</groupId>
        <artifactId>cui-java-parent</artifactId>
        <version>1.5.11</version>
        <relativePath />
    </parent>
    <groupId>de.cuioss.sheriff.token</groupId>
    <artifactId>token-sheriff-parent</artifactId>
    <version>0.9.5-SNAPSHOT</version>
    <packaging>pom</packaging>
    <!-- Must be declared: <inceptionYear> is inherited, and the license plugin binds
         the copyright ${year} to it. Without this, this project would report
         cui-parent-pom's 2022 and restamp every header on each -Ppre-commit run. -->
    <inceptionYear>2025</inceptionYear>
    <name>Token-Sheriff Parent</name>
    <description>A comprehensive framework for validating OAuth/JWT tokens in multi-issuer environments.
        The module provides robust token parsing, validation, and management capabilities
        with a focus on security and ease of use, leveraging standard JDK cryptographic providers.
    </description>

    <url>https://github.com/cuioss/TokenSheriff/</url>
    <modules>
        <module>bom</module>
        <module>token-sheriff-validation</module>
        <module>token-sheriff-client</module>
        <module>token-sheriff-quarkus-parent</module>
        <module>benchmarking</module>
    </modules>
    <scm>
        <url>https://github.com/cuioss/TokenSheriff/</url>
        <connection>
            scm:git:https://github.com/cuioss/TokenSheriff.git
        </connection>
        <developerConnection>
            scm:git:https://github.com/cuioss/TokenSheriff/
        </developerConnection>
        <tag>HEAD</tag>
    </scm>
    <issueManagement>
        <url>https://github.com/cuioss/TokenSheriff/issues</url>
        <system>GitHub Issues</system>
    </issueManagement>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <version.quarkus>3.39.1</version.quarkus>
        <frontend.node.version>v22.22.0</frontend.node.version>
        <frontend.npm.version>10.9.2</frontend.npm.version>
        <!-- TEMPORARY: overrides the 2.1.0 value inherited from de.cuioss:cui-java-bom so this
             project can consume the verifyHostname knob from cui-http PR #167 ahead of its release.
             REMOVAL CONDITION: drop this property override, the <repositories> block, and the
             maven-enforcer-plugin execution below together once cui-http ships a release
             carrying PR #167. -->
        <version.cui.http>2.2-SNAPSHOT</version.cui.http>
    </properties>

    <!-- TEMPORARY: the Central snapshots channel, needed only to resolve the
         de.cuioss:cui-http:2.2-SNAPSHOT pin above. <releases> is disabled so the release channel
         is untouched and only snapshot artifacts are looked up here.
         REMOVAL CONDITION: drop this <repositories> block, the version.cui.http property override,
         and the maven-enforcer-plugin execution below together once cui-http ships a release
         carrying PR #167. -->
    <repositories>
        <repository>
            <id>central</id>
            <url>https://central.sonatype.com/repository/maven-snapshots/</url>
            <releases>
                <enabled>false</enabled>
            </releases>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
    </repositories>

    <build>
        <plugins>
            <!-- TEMPORARY: fails the build at validate when any module resolves a cui-http older
                 than 2.2-SNAPSHOT, so a silent fallback to the BOM-inherited 2.1.0 surfaces as a
                 banned-dependency error rather than compiling green against the pre-PR-#167 API.
                 REMOVAL CONDITION: drop this execution, the version.cui.http property override,
                 and the <repositories> block together once cui-http ships a release
                 carrying PR #167. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>3.6.3</version>
                <executions>
                    <execution>
                        <id>ban-pre-verify-hostname-cui-http</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <bannedDependencies>
                                    <excludes>
                                        <exclude>de.cuioss:cui-http:(,2.2-SNAPSHOT)</exclude>
                                    </excludes>
                                </bannedDependencies>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
